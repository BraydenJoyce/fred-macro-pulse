# Copy this to secrets.toml and fill in real values,
# OR paste these into the Streamlit Cloud "Secrets" UI.

# GitHub personal access token with actions:read scope
GH_TOKEN = "ghp_xxxxxxxxxxxxxxxxxxxx"

# Your GitHub repo in "owner/repo" format
GITHUB_REPO = "your-username/fred-macro-pulse"
